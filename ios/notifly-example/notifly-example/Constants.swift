enum NotiflyConfig {
    // Change these values to your own
    // You can find your project id in the notifly setting page (https://notifly.tech/console/settings?environment=0)
    static let projectId: String = "{YOUR_PROJECT_ID}"
    static let username: String = "{YOUR_USERNAME}"
    static let password: String = "{YOUR_PASSWORD}"
}
